import React from 'react';

const Footer = () => {
    return (
        <footer>
            <p>&copy; Pizza's Store {new Date().getFullYear()}</p>
        </footer>
    );
};

export default Footer;
